name: Keep API Alive

on:
  schedule:
    - cron: '* * * * *' # Runs every 5 minutes

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping API
        run: |
          curl -s https://resume-pdf-endpoint.onrender.com/
